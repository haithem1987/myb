################################################################################
# Kustomize base – all shared service manifests (namespace-agnostic)
# References service directories as sub-kustomizations (required by kustomize
# security model – cannot reference files above the kustomization root)
################################################################################
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Credentials are provisioned separately. The files under ../secrets are
  # Git-safe templates and must never be applied by the normal deployment.
  - ../ingress

  # Infra
  - ../services/rabbitmq

  # Application services
  - ../services/keycloak
  - ../services/admin
  - ../services/client
  - ../services/coproperty
  - ../services/invoice
  - ../services/mailer
  - ../services/notification

labels:
  - pairs:
      project: myb
      managed-by: kustomize
