apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Intentionally empty. Files in this directory are Git-safe templates only.
  # Provision live database, Keycloak, registry, and SMTP credentials through
  # the corresponding setup scripts or your secret manager. Never include the
  # placeholder manifests in the normal Kustomize deployment.
