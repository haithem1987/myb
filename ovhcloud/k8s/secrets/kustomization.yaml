apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - database-secrets.yaml
  - keycloak-secrets.yaml
  - registry-secret.yaml
# SMTP is provisioned separately with scripts/setup-gmail-smtp.sh so a normal
# kustomize deployment never overwrites the live credential with a repository
# placeholder.
