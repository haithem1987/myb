# SMTP Secrets for Mailer Service
# IMPORTANT: Configure with your SMTP provider (SendGrid, Mailgun, etc.)

apiVersion: v1
kind: Secret
metadata:
  name: smtp-credentials
  namespace: myb-platform
type: Opaque
stringData:
  SMTP_HOST: "smtp.sendgrid.net"
  SMTP_PORT: "587"
  SMTP_USERNAME: "apikey"
  SMTP_PASSWORD: "jfqrysgrmshbqkfy"
  EMAIL_FROM_ADDRESS: "benmaadnidhal96@gmail.com"
  EMAIL_FROM_NAME: "MYB Platform"
  SMTP_ENABLE_SSL: "true"
  SMTP_STARTTLS: "true"
